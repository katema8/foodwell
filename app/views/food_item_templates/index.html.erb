<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All food items
    </h1>

    <a href="/food_items/new" class="btn btn-block btn-outline-secondary">
      Add a new food item
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "food_item_templates/food_items_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Food item
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @food_items.each do |food_item| %>
      <tr>
        <!-- Display food_item.id start -->
        <td>
          <%= food_item.id %>
        </td>
        <!-- Display food_item.id end -->

        <!-- Display food_item.food_item start -->
        <td>
          <%= food_item.food_item %>
        </td>
        <!-- Display food_item.food_item end -->

        <!-- Display food_item.created_at start -->
        <td>
          <%= time_ago_in_words(food_item.created_at) %> ago
        </td>
        <!-- Display food_item.created_at end -->

        <!-- Display food_item.updated_at start -->
        <td>
          <%= time_ago_in_words(food_item.updated_at) %> ago
        </td>
        <!-- Display food_item.updated_at end -->

        <td>
          <a href="/food_items/<%= food_item.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @food_items, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
